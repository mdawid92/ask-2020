# requirements

You need to have virtualbox and vagrant installed.

# boot VM

to boot machine run
```bash
vagrant up
```

# usage
To login to VM run
```bash
vagrant ssh
```

BTRFS volume should be mounted on `/data` path.